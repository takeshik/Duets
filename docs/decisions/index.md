# ADR Index

| # | Title | Keywords | Abstract |
|---|-------|----------|----------|
| [ADR-1](1_design-documentation-strategy.md) | Design Documentation Strategy | documentation, ADR, AGENTS.md, AI agent, context window, architecture | Establishes a 3-layer documentation structure (AGENTS.md / architecture.md / ADRs) for a solo-developer + AI agent project, prioritizing agent context efficiency and structured readability over keeping everything in a single file. |
| [ADR-2](2_use-typescript-as-the-scripting-language.md) | Use TypeScript as the Scripting Language | TypeScript, scripting, transpilation, d.ts, completions, JavaScript | Chooses TypeScript over a custom DSL, C# scripting (Roslyn), Lua, and plain JavaScript; its static type system maps naturally to .NET types enabling .d.ts-based completions, while the compile-to-JavaScript design provides a clear implementation path through the official TS compiler on Jint. |
| [ADR-3](3_use-httplistener-instead-of-asp-net-core-kestrel.md) | Use HttpListener Instead of ASP.NET Core / Kestrel | HTTP, HttpListener, ASP.NET Core, embeddability, dependencies, Unity, mobile | Selects System.Net.HttpListener over ASP.NET Core/Kestrel and third-party alternatives to preserve universal embeddability in constrained environments (Unity, Godot, iOS, Android); HttpListener is part of the BCL with zero additional dependencies, at the cost of requiring manual routing and middleware. |
| [ADR-4](4_use-jint-as-the-javascript-engine.md) | Use Jint as the JavaScript Engine | Jint, JavaScript engine, CLR interop, AOT, pure .NET, sandboxing | Confirms Jint over V8/ClearScript, Node.js hosting, and other managed engines; its pure C# implementation satisfies the universal embeddability requirement, while strong CLR interop (AllowClr) and built-in sandboxing meet the remaining drivers. Startup cost for large scripts like typescript.js is the primary trade-off. |
| [ADR-5](5_separate-jint-engines-for-typescript-compiler-and-user-code.md) | Separate Jint Engines for TypeScript Compiler and User Code | Jint, engine isolation, TypeScriptService, ScriptEngine, AllowClr, security | Runs the TypeScript compiler and user scripts in two independent Jint engine instances; isolation prevents user code from polluting compiler global state, and each engine requires a different security policy (compiler sandboxed; user engine uses AllowClr). |
| [ADR-6](6_fetch-and-cache-runtime-js-assets-from-cdn.md) | ~~Fetch and Cache Runtime JS Assets from CDN~~ *(Deprecated — superseded by ADR-18)* | CDN, unpkg, caching, typescript.js, monaco, assembly size | Originally established fetching typescript.js and monaco loader.js from unpkg at runtime and caching in the system temp directory for 7 days; superseded by ADR-18, which introduces a pluggable IAssetSource abstraction while preserving this default. |
| [ADR-7](7_use-monaco-editor-as-the-browser-based-repl-ui.md) | Use Monaco Editor as the Browser-based REPL UI *(Partially superseded by ADR-32)* | Monaco, REPL, browser, VS Code, embedded resources, completions, DuetsPad | Selects Monaco Editor over a VS Code extension approach; the Monaco editor decision remains in force as DuetsPad's Editor surface, while ADR-32 supersedes the browser REPL UI and ReplService framing with the DuetsPad browser debug pad. |
| [ADR-8](8_use-addextralib-to-inject-dts-declarations-for-completions.md) | Use addExtraLib to Inject .d.ts Declarations for Completions | Monaco, addExtraLib, d.ts, completions, ClrDeclarationGenerator, SSE | Uses Monaco's built-in TypeScript language service with addExtraLib over external tsserver or Jint runtime inspection; ClrDeclarationGenerator produces .d.ts from .NET types via reflection and declarations are pushed to Monaco via SSE. Unmappable .NET patterns fall back to `any`. |
| [ADR-9](9_wrap-httplistener-in-a-dedicated-middleware-library.md) | Wrap HttpListener in a Dedicated Middleware Library | HttpHarker, middleware, HttpListener, composability, separation of concerns | Extracts HTTP infrastructure into a separate middleware-based library (HttpHarker) with no dependency on Duets; raw HttpListener usage did not scale for multiple web interfaces, and the Rin pattern of serving UI as composable middleware inspired the design. |
| [ADR-10](10_extract-itranspiler-interface-for-scriptengine.md) | Extract ITranspiler Interface for ScriptEngine | ITranspiler, ScriptEngine, TypeScriptService, interface, dependency injection, testability | Introduces ITranspiler as the minimal interface between ScriptEngine and its transpiler; ScriptEngine requires it at construction and always transpiles before executing, encapsulating the transpile-then-execute pattern and enabling stub transpilers in tests without the full TypeScript compiler. |
| [ADR-11](11_sandbox-multi-mode-debugging-cli.md) | Sandbox as a Multi-Mode Debugging CLI *(Partially superseded by ADR-16)* | Duets.Sandbox, batch mode, JSONL, AI agent, REPL, CLI | Transforms Duets.Sandbox into a multi-mode CLI (REPL, eval, complete, serve, batch); the JSONL batch mode lets AI coding agents pipe operations to stdin and read results from stdout without background process management, with state persisting across operations within a session. |
| [ADR-12](12_language-service-host-rewrite-and-nolib.md) | Rewrite LanguageServiceHost and Provide Standard Library via CDN | LanguageServiceHost, language-service.js, lib.es5.d.ts, completions, InjectStdLibAsync | Fully rewrites the TypeScript LanguageServiceHost to implement all methods required by createLanguageService, fixing a crash on repeated GetCompletions calls; adds opt-in injection of lib.es5.d.ts from CDN via InjectStdLibAsync() to enable completions for JavaScript built-ins alongside registered .NET types. |
| [ADR-13](13_script-built-ins-and-typings-object.md) | ~~Script Built-ins and the `typings` Object~~ *(Method names superseded by ADR-24)* | typings, ScriptBuiltins, ScriptTypings, RegisterTypeBuiltins, useNamespace, scanAssembly | Introduces the typings global object in the Duets core library, replacing per-host importTypeDefs wiring and making type-declaration management reusable across any embedding application. Method names superseded by ADR-24. |
| [ADR-14](14_assembly-derivation-from-type-reference.md) | Assembly Derivation from TypeReference via `scanAssemblyOf` / `useAssemblyOf` | scanAssemblyOf, useAssemblyOf, TypeReference, AllowSystemReflection, typings API | Adds scanAssemblyOf and useAssemblyOf to ScriptTypings, deriving the assembly from a CLR TypeReference in C# rather than in script, sidestepping the AllowSystemReflection requirement while keeping the call ergonomic when a type reference is already in scope. |
| [ADR-15](15_namespace-skeleton-dummy-member.md) | Namespace Skeleton Visibility via `$name` Dummy Member | scanAssembly, namespace skeleton, $name, TypeScript language service, completions | Resolves TypeScript's filtering of empty declare namespace blocks by injecting a `const $name` dummy member into skeletons produced by scanAssembly; the member is automatically removed (skeleton overwritten with empty declaration) when a real type for that namespace is registered. |
| [ADR-16](16_samples-directory-and-sandbox-role-clarification.md) | Introduce `samples/` Directory and Clarify Duets.Sandbox Role | samples, file-based apps, Duets.Sandbox, dotnet run, role clarification | Introduces a samples/ directory of self-contained file-based .cs examples (dotnet run samples/<file>.cs), separating usage examples from internal tooling; simultaneously removes the eval command from Duets.Sandbox so all remaining commands use the fully-initialized SandboxSession uniformly. |
| [ADR-17](17_versioning-strategy-and-ci.md) | ~~Versioning Strategy and CI Pipeline~~ *(Superseded by ADR-23)* | Nerdbank.GitVersioning, NBGV, SemVer, GitHub Actions, NuGet, CI | Adopts Nerdbank.GitVersioning (NBGV) for automated versioning from Git history; releases are triggered by v{major}.{minor}.{patch} tags and publish to GitHub Packages, while development builds carry a -dev.{height}.g{commit} prerelease suffix for traceability. |
| [ADR-18](18_pluggable-asset-source-abstraction.md) | Pluggable Asset Source Abstraction (`IAssetSource`) *(Return type partially superseded by ADR-37)* | IAssetSource, AssetSources, asset fetching, CDN, offline, testability, HttpClient | Lifts the YAGNI deferral from ADR-6 by introducing IAssetSource and AssetSources factory to make runtime JS asset fetching pluggable (HTTP, unpkg, embedded resources, delegates, composable disk cache); default behavior is identical to ADR-6, but consumers in air-gapped environments can now supply assets without forking the library. |
| [ADR-19](19_babel-transpiler-as-typescript-7-migration-path.md) | Babel Transpiler as TypeScript 7 Migration Path | BabelTranspiler, Babel, @babel/standalone, TypeScript 7, Go rewrite, ITranspiler, migration | Introduces BabelTranspiler — an ITranspiler implementation backed by @babel/standalone running in Jint — as the forward-compatibility path for TypeScript 7, which ships only a native Go binary with no JS API; chosen over ts-blank-space (no enum/namespace support), sucrase (no standalone bundle, broken namespace output), and TypeScript 6.x freeze (dead end). |
| [ADR-20](20_dump-as-global-function-not-prototype-extension.md) | `dump` Implemented as a Generic Global Function, Not a Prototype Extension *(Ownership/options partially superseded by ADR-35)* | dump, Object.prototype, Monaco, addExtraLib, interface Object, generics, completions | Implements `dump<T>(value: T): T` as a global function rather than an `Object.prototype` extension because Monaco's `addExtraLib` does not propagate `interface Object` augmentations to specific inferred types, making prototype-based completion discovery impossible without losing type precision. |
| [ADR-21](21_typings-import-namespace.md) | ~~`typings.importNamespace` — Namespace Import with Completion Registration~~ *(Superseded by ADR-24)* | typings, importNamespace, AllowClr, NamespaceReference, Jint internals, completions | Adds `typings.importNamespace(ns)` to combine runtime namespace access and TypeScript completion registration in one call; superseded by ADR-24 which corrects the original `using` semantics premise and redesigns the full typings API. |
| [ADR-22](22_target-framework-selection.md) | Target Framework Selection — `netstandard2.1` and `net8.0` | TFM, netstandard2.1, net8.0, Unity, Godot, multi-targeting, platform coverage | Library projects target netstandard2.1 and net8.0; NS2.1 covers Unity 2021.2+/Xamarin/Mono, net8.0 covers Godot 4.4+. NS2.0 excluded (no .NET Framework target; all current Unity LTS versions support NS2.1). net6.0 and net10.0 excluded (no per-TFM capability gain). |
| [ADR-23](23_ci-and-package-publishing.md) | CI Pipeline and NuGet Package Publishing Strategy | CI, GitHub Actions, NuGet, GitHub Packages, IsPackable, HttpHarker, snapshot, release | Publishes Duets and HttpHarker as two independent NuGet packages (HttpHarker is a declared dependency of Duets, not bundled). Every push to main publishes a snapshot pre-release to GitHub Packages; tag pushes produce release packages with an overridden clean version number. |
| [ADR-24](24_typings-api-redesign.md) | `typings` API Redesign — `usingNamespace` and `import-` Prefix | typings, usingNamespace, importType, importAssembly, importNamespace, using semantics, API rename | Adds `typings.usingNamespace(ns)` implementing C# `using` semantics (scatter types as globals + completions); renames `use-` prefix methods to `import-` for consistency; removes `useNamespace` as superseded. Supersedes method names in ADR-13 and the `useNamespace`/`importNamespace` split in ADR-21. |
| [ADR-25](25_session-as-canonical-entry-point.md) | Session as Canonical Entry Point | DuetsSession, initialization, BabelTranspiler, TypeDeclarations, ScriptEngine, session, concurrency, declaration store | Introduces DuetsSession as the canonical entry point owning TypeDeclarations, ITranspiler, and ScriptEngine; extracts TypeDeclarations from TypeScriptService as an independent store; BabelTranspiler becomes the default (removing declarations from the common path); multiple sessions provide first-class isolated contexts without shared locking. |
| [ADR-26](26_extension-method-support-via-member-accessor-hook.md) | Extension Method Support via `MemberAccessor` Hook and `ExtensionMethodRegistry` | extension methods, MemberAccessor, ExtensionMethodRegistry, addExtensionMethods, LINQ, generic dispatch, interface augmentation | Implements runtime extension method dispatch and TypeScript completion declarations via `typings.addExtensionMethods(type)`; uses Jint's `MemberAccessor` hook (rather than the construction-time `AddExtensionMethods`) to allow post-construction registration; introduces `ExtensionMethodRegistry` for thread-safe dispatch, and `ClrDeclarationGenerator.GenerateExtensionMethodsTs` for interface augmentation declarations. |
| [ADR-27](27_split-javascript-runtime-backends-from-duets-core.md) | Split JavaScript Runtime Backends from Duets Core | Jint, ScriptEngine, ScriptValue, backend packages, runtime abstraction | Moves runtime-specific implementations out of `Duets` core into dedicated backend packages starting with `Duets.Jint`, keeping session/transpiler concepts engine-agnostic and leaving the door open for additional backends. |
| [ADR-28](28_unified-createasync-api-and-backend-autodiscovery.md) | Unified `CreateAsync` API and Backend Auto-Discovery via `DuetsBackendRegistry` | DuetsSession, CreateAsync, DuetsSessionConfiguration, UseTranspiler, UseBabel, DuetsBackendRegistry, ModuleInitializer, discovery | Moves transpiler selection into `DuetsSessionConfiguration` alongside the engine, making both axes symmetric; introduces `DuetsBackendRegistry` for `[ModuleInitializer]`-based default registration so that referencing `Duets.Jint` alone enables `CreateAsync()` with no arguments. |
| [ADR-29](29_jsdoc-provider-abstraction.md) | JSDoc Provider Abstraction for CLR Declaration Generator | IJsDocProvider, JsDocProviders, XmlDocumentationProvider, JSDoc, XML documentation, NuGet, composite, declaration refresh | Introduces `IJsDocProvider` and a `JsDocProviders` composite to supply .NET XML documentation as TypeScript JSDoc annotations; supports multiple async sources with per-provider exception isolation and a `ProviderAdded` event for automatic declaration refresh. |
| [ADR-30](30_scriptvalue-redesign-abstract-class-and-jstype.md) | ScriptValue Redesign — Abstract Class | ScriptValue, IScriptValueAdapter, abstract class, equality, backend, Okojo | Replaces the adapter-pattern `ScriptValue` (sealed class + `IScriptValueAdapter` vtable) with an abstract class hierarchy; `==`/`!=` work correctly across sentinels and engine-backed values; cross-backend comparisons throw. |
| [ADR-31](31_scriptengine-generic-backend-base-and-iscriptengine.md) | ScriptEngine Generic Backend Base and IScriptEngine Interface | ScriptEngine, IScriptEngine, ScriptEngine<TValue>, IScriptValueConverter, backend, generics, interface, SetValue, Evaluate | Replaces the non-generic abstract `ScriptEngine` class with a two-layer design: `IScriptEngine` (interface — public contract held by callers) and `ScriptEngine<TValue>` (generic abstract class — the base backends inherit from). `ScriptEngine<TValue>` holds an `IScriptValueConverter<TValue>` and uses it to implement `SetValue(string, ScriptValue)` and the `Evaluate*` return-value wrapping concretely, removing these responsibilities from backend subclasses. |
| [ADR-32](32_duetspad-supersede-replservice-with-new-output-model.md) | DuetsPad Supersedes ReplService as the Browser Debug Pad | DuetsPad, ReplService, browser UI, debug pad, LINQPad, Editor, Canvas, Timeline, Immediate | Introduces DuetsPad as the named successor to ReplService: a browser debug pad with Editor, Canvas, Timeline, and Immediate surfaces where Timeline owns structured history output, including Immediate evaluation results, while Canvas owns persistent display state. |
| [ADR-33](33_tabler-css-framework-for-duetspad.md) | Adopt Tabler 1.x as the CSS Framework for DuetsPad | DuetsPad, Tabler, Bootstrap 5, CSS framework, IAssetSource, icons, dark mode, admin UI | Chooses Tabler 1.x for DuetsPad's admin-application visual vocabulary because it provides Bootstrap-compatible components, dark mode, icons, MIT licensing, and static assets compatible with `IAssetSource` without adopting Tabler's page-layout templates. |
| [ADR-34](34_duetspad-session-ownership-and-isolation.md) | DuetsPad Session Ownership and Isolation | DuetsPadSession, DuetsSession, session isolation, sessionStorage, Canvas, Timeline, SSE, interaction store, browser session | Introduces DuetsPadSession as the owner of one browser session's DuetsSession, Canvas, Timeline, object renderers, DuetsPad script globals, SSE subscribers, and the per-session interaction store. SessionRegistry owns session identifiers, create/lookup/delete, and idle reclamation; SessionBootstrap performs construction-time script-global wiring; DuetsPadService is a thin HTTP router delegating to these collaborators. |
| [ADR-35](35_duetspad-rendering-model.md) | DuetsPad Rendering Model — Render Nodes, Object Renderers, Render Context, and Dump Ownership | DuetsPad, render node, IRenderNode, ITerminalRenderNode, Element, RawHtml, IObjectRenderer, RenderContext, DumpOptions, DisplayContent, dump, depth limit, cycle detection, dump ownership, object rendering | Defines the full DuetsPad output pipeline: structured render nodes reduced to terminal nodes before entering Canvas or Timeline; session-scoped object renderers with last-wins resolution and a default renderer fallback; a RenderContext that carries DumpOptions (MaxDepth=5, MaxItems=1000) and centralizes depth limiting and cycle detection across renderer boundaries; DisplayContent as the render result (terminal body plus interactions); and the dump-as-DuetsPad-only-global decision. Merges the former render-node and render-context/dump-ownership decisions into a single canonical rendering ADR. |
| [ADR-36](36_duetspad-server-canonical-output-protocol.md) | DuetsPad Server-Canonical Output Protocol *(Canvas event shape amended by ADR-43, ADR-45)* | DuetsPad, Canvas, Timeline, server-canonical state, SSE, multiplexed stream, protocol, event types, reconnect | Makes Canvas and Timeline server-canonical DuetsPad state projected to the browser, and carries every server-to-browser event for a session over a single multiplexed per-session SSE stream (`GET /sessions/{id}/events`) rather than one stream per surface — avoiding the HTTP/1.1 per-origin connection cap and giving a deterministic initial-burst order. Event types are namespaced by surface and operation: `canvas.snapshot`, `canvas.replace`, `timeline.reset`, `timeline.append`, `timeline.update`, `timeline.trim`, `type.declaration`, and `control.*` (ADR-42). |
| [ADR-37](37_binary-first-iassetsource.md) | Binary-First `IAssetSource` Asset Content | IAssetSource, GetBytesAsync, GetStringAsync, binary assets, web fonts, Tabler Icons, woff2, offline | Changes `IAssetSource` from text-only (`GetAsync: Task<string>`) to binary-first (`GetBytesAsync: Task<byte[]>`) with a UTF-8 `GetStringAsync` extension, so DuetsPad can serve binary web-font assets (Tabler Icons woff2 per ADR-33) through the same pluggable, cached, offline-capable abstraction introduced in ADR-18. Text consumers decode via the extension and default behavior is unchanged. Amends the string-return decision of ADR-18. |
| [ADR-38](38_duetspad-session-lifecycle.md) | DuetsPad Session Lifecycle — Explicit Disposal and Idle Reclamation | DuetsPad, DuetsPadSession, lifecycle, DELETE, idle timeout, keepalive, activity, disconnect, multi-browser | Resolves the lifecycle questions ADR-34 left open. `DELETE /sessions/{sessionId}` disposes a session (returning `{ok:true}`); disposed identifiers are never reused and answer the uniform unknown-session response thereafter. The service reclaims sessions with no activity within a configurable idle timeout, where creation, evaluation, and SSE keepalive/stream activity all count as activity. Browser disconnect alone never disposes a session, and multiple browser attachment to one session is not first-class supported. |
| [ADR-39](39_duetspad-timeline-quota-policy.md) | DuetsPad Timeline Quota Policy | DuetsPad, Timeline, quota, timeline.trim, TimelineEntryLimit, drop-oldest, removeBeforeId, entry id, marker | Defines the quota policy ADR-36 left open for the `timeline.trim` verb. `DuetsPadServiceOptions.TimelineEntryLimit` (`int?`, `null` = unlimited default, `<= 0` rejected) caps retained entry count; the server drops oldest entries after an append, broadcasting `timeline.append` then `timeline.trim` (boundary `removeBeforeId` = first retained entry's id, `marker` always null). Entry ids are never reused (`NextId` preserved across trim) so the browser projection converges; reconnecting subscribers get the trimmed state via `timeline.reset`. |
| [ADR-40](40_duetspad-default-object-classification-policy.md) | DuetsPad Default Object Classification Policy | DuetsPad, default renderer, classification, conceptual shape, named-member object, map, JS object, ExpandoObject, marshaling | Fixes how the default object renderer (ADR-35) classifies a value: by conceptual shape, not by the accidental CLR type engine marshaling produces. A JS object literal is classified as a named-member object — converging with ordinary CLR objects — even though Jint marshals it to a dictionary-like dynamic value (`ExpandoObject`); genuine maps (`IDictionary`/`IDictionary<,>`/`IReadOnlyDictionary<,>`/`Map`) stay a distinct classification. Concrete presentation (HTML/CSS, table/grid/list shape, null/enum/nesting/numeric formatting) is left to implementation and tests, not fixed by the ADR. |
| [ADR-41](41_duetspad-interaction-model.md) | DuetsPad Interaction Model | DuetsPad, interaction, ui.button, DisplayContent, PendingInteraction, CommittedInteraction, InteractionStore, DisplayPath, handler id, live, stale, invoke endpoint | Decides the interaction mechanism ADR-32/ADR-35 deferred. Rendering returns `DisplayContent` (terminal body plus interactions), keeping ADR-35's node tree display-only; server-side handlers are addressed by an opaque id and triggered via `POST /sessions/{id}/interactions/{handlerId}/invoke`, with a `stale` answer for clicks that race a retire. Handlers live in a per-session `InteractionStore` (owned by DuetsPadSession per ADR-34), are committed with `Live`/`Stale` state, and are released when their Canvas/Timeline output is replaced, trimmed (ADR-39), or the session is disposed. |
| [ADR-42](42_duetspad-pad-control-surface-and-command-channel.md) | DuetsPad `pad` Control Surface and Imperative Command Channel | DuetsPad, pad global, control channel, control.*, imperative command, resetSession, openText, setEditorText, browser-driven reset, eventual consistency, popup fallback | Adds a `pad` script global (the DuetsPad host-command surface) and a server-to-browser `control.*` imperative command channel on the multiplexed per-session stream (ADR-36). `pad.resetSession` / `openText` / `setEditorText` enqueue commands buffered during a run and flushed after it while the eval gate is held; reset is browser-driven (the engine never disposes itself mid-eval, and session identity coinciding with stream identity removes the need for a generation id), `openText` presents an open action with a popup-block toast fallback and hands the text off out-of-band (the URL carries only an opaque handoff id, never the text), and editor text is set-only because the server holds no live editor buffer. `load`/`save` and `openFile` are reserved for the script-persistence work. |
| [ADR-43](43_duetspad-named-multi-canvas.md) | DuetsPad Named Multi-Canvas | DuetsPad, canvas, canvases, named canvas, getOrAdd, default alias, canvas.snapshot, canvas.replace, name field, tabbed pane | Lets one DuetsPad session own multiple named canvases. A new top-level `canvases` global exposes `get(name)` with getOrAdd semantics returning the existing `add`/`set`/`clear` surface; `canvas` is retained as an alias for `canvases.get("default")` and the `"default"` canvas always exists. Canvas state and interactions (ADR-41) are keyed by name; the `canvas.snapshot`/`canvas.replace` events (ADR-36) gain a `name` field with one snapshot per canvas in the initial burst — preserving the event types, single multiplexed stream, and burst ordering. The browser Canvas pane becomes tabbed (sub-tabs in split view, promoted to flat top-level tabs in tabbed view). Amends the canvas-event shape of ADR-36. |
| [ADR-44](44_tagged-template-completion-rpc-boundary.md) | Tagged-Template Completion RPC Boundary | tagged-template, completion, Monaco, DuetsPad, RPC, callback, SSE, TypeScript tokenizer | Makes tagged-template completion a thin server RPC to registered host providers while Monaco owns editor-context detection, rejecting a server-side JavaScript lexer because it adds parser-grade maintenance cost without meaningful security benefit. |
| [ADR-45](45_duetspad-canvas-incremental-patch-protocol.md) | DuetsPad Canvas Incremental Patch Protocol | DuetsPad, Canvas, canvas.patch, revision, CanvasDiffer, incremental patch, preflight, resync, interaction set | Adds revisioned Canvas projection and a conservative positional `canvas.patch` protocol. The server diffs the last projected state, falls back to full replace when larger, validates interaction candidates before commitment, and exposes canvas-scoped resync snapshots; the browser enforces revision gates, preflight-then-mutate patch atomicity, full interaction-set validation, bounded resync buffers, and security-policy checks mirroring render-node validation. |
